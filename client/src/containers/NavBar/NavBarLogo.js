import styles from './header.module.css'

const HeaderLogo = () => {
    return (
        <div className= {styles.navBar__logo} >
            PlatformAi
        </div>
    );
};

export default HeaderLogo;